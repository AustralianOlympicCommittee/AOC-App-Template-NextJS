{
  "name": "aoc-app-template-nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "check:scripts": "node --check scripts/app-contract.mjs && node --check scripts/generate-deployment-manifest.mjs && node --check scripts/migrate-lakebase-audit.mjs && node --check scripts/provision-lakebase.mjs && node --check scripts/validate-app-contract.mjs",
    "validate:contract": "node scripts/validate-app-contract.mjs",
    "validate:manifest": "node scripts/generate-deployment-manifest.mjs --environment dev",
    "generate:manifest": "node scripts/generate-deployment-manifest.mjs",
    "migrate:audit": "node scripts/migrate-lakebase-audit.mjs",
    "db:migrate": "npm run migrate:audit",
    "check": "npm run validate:contract && npm run check:scripts && npm run validate:manifest && npm run typecheck && npm run build"
  },
  "dependencies": {
    "next": "16.2.9",
    "pg": "8.22.0",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "overrides": {
    "postcss": "8.5.10"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/pg": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest"
  }
}
