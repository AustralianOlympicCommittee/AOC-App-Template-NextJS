# User Guide

This guide should explain how end users use the adopted application.

The current repository is still a template, so app-specific user workflows are not yet defined.

## Intended Users

Record the intended audience in `app.yml`.

## Main Workflows

Document app workflows here during adoption or implementation.

| Workflow | Who Uses It | Outcome |
| --- | --- | --- |
| To be defined | To be defined | To be defined |

## Access

Access is controlled through Microsoft Entra groups:

- Read access: `app-{app}-{env}-read`
- Write access: `app-{app}-{env}-write`
- Admin access: `app-{app}-{env}-admin`

## Support

See [support.md](support.md).
